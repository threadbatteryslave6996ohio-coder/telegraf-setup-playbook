[defaults]
# Single shared inventory at the project root. Override with
# -i inventory.tailscale.ini to reach hosts over Tailscale.
inventory = inventory.ini
interpreter_python = auto_silent
stdout_callback = default
result_format = yaml
bin_ansible_callbacks = True

[privilege_escalation]
become = True
become_method = sudo
become_ask_pass = False
