# Same as inventory.ini but reaches hosts by their Tailscale MagicDNS names.
# Use only after a host has joined the tailnet. Select with -i inventory.tailscale.ini.

[arch_hosts]
# add Arch hosts by their Tailscale name here

[arch_hosts:vars]
ansible_user=ansible

[ubuntu_hosts]
clippy-server ansible_host=clippy-server

[ubuntu_hosts:vars]
ansible_user=ansible
